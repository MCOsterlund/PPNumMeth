using static System.Console;
using static System.Math;
public static class math{
	public static void Main(){
		double sqrt2=Sqrt(2.0), exppi=Pow(PI,E), piexp=Pow(E,PI);
		Write($"sqrt2={sqrt2} exppi={exppi} piexp={piexp}\n");
	}
}
